/**
 * Stub for react-dom/server for type checking
 * This is used to resolve TypeScript errors when the actual module is not available
 */

/**
 * Renders a React element to its initial HTML
 */
export function renderToString(element: any): string {
  return "";
}
