/**
 * PageSpeed Insights - EmulatedFormFactor
 * @description The form factor the emulation should use.
 * @category (Analyzer) [PageSpeed Insights]
 */
export enum EmulatedFormFactor {
  /**
   * Form factor is unknown.
   */
  UNKNOWN_FORM_FACTOR,
  /**
   * Form factor is 'desktop'.
   */ desktop,
  /**
   * Form factor is 'mobile'.
   */ mobile,
  /**
   * No form factor.
   */ none,
}
