/**
 * PageSpeed Insights - Timing
 * @description Timing information for this LHR.
 * @category (Analyzer) [PageSpeed Insights]
 */
export interface Timing {
  /**
   * The total duration of Lighthouse's run.
   */
  total: number;
}
